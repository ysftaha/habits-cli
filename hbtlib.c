#include "hbtlib.h"
#include <stdio.h>

/**
 * @brief Receives a habit struct and inserts it into the database
 * @param habit a habit to be inserted into both the Habits table for lookup and
 * into it's own newly created table.
 * @todo error reporting
 * @see @ref habit
 */
bool add(habit *hbt) { return 0; }
bool rem(habit *hbt) { return 0; }
